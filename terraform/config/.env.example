# AWS credentials for Makefile (NOT for Terraform variables)
# Copy this file to .env and customize

# AWS CLI profile name (from ~/.aws/credentials)
AWS_PROFILE=default

# AWS region to deploy resources
AWS_REGION=us-east-1
